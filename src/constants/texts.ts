/**
 * 异常提示语统一管理（架构 §5.8：文案集中在 `types/enums.ts` 文案表 + 本文件）。
 *
 * 依据 PRD P0-9（识别失败态）与 §6.4（确认状态异常提示）。
 */

/** 识别失败 / 运行异常提示文案。 */
export const TEXTS = {
  // ---- P0-9 识别失败态 ----
  /** 画面暂不可读。 */
  UNREADABLE_PICTURE: '画面暂不可读',
  /** 等待新的清晰读数。 */
  WAIT_CLEAR_READING: '等待新的清晰读数',
  /** 请保持游戏画面可见。 */
  KEEP_VISIBLE: '请保持游戏画面可见',

  // ---- 校准相关（P0-14 / P0-15）----
  /** 校准页未选窗口提示。 */
  NEED_WINDOW: '请先回到控制面板选择游戏窗口',
  /** 正在确认各项位置。 */
  CONFIRMING_REGIONS: '正在确认各项位置',
  /** 缺少有效的界面定位。 */
  NO_VALID_LOCATION: '缺少有效的界面定位',
  /** 文字无法解析。 */
  TEXT_UNPARSABLE: '文字无法解析',
  /** 文字数值与血条比例不一致。 */
  INCONSISTENT_WITH_BAR: '文字数值与血条比例不一致',

  // ---- 识别置信度提示（架构 §9.3 第 4 条）----
  /**
   * 置信度偏低时的提示。
   *
   * 注意：本向导（`GlyphWizardDialog`）P0 只支持「重建内置模板库 / 切换内置与自定义」，
   * **不支持**用真实游戏画面逐字符重采（属 P1）。因此此处文案必须描述
   * 向导**实际提供**的能力，避免引导用户去做一件当前做不到的事。
   */
  LOW_CONFIDENCE_HINT: '识别置信度偏低，可尝试重建内置模板库，或检查框选区域是否有遮挡',

  // ---- 后台节流提示（架构 R1）----
  /** 后台采样率下降提示。 */
  BACKGROUND_THROTTLED: '采样率可能下降，经验/小时 仍按真实时间计算',
  /** 采样不足提示。 */
  SAMPLE_NOT_ENOUGH: '采样不足',

  // ---- 记录状态相关 ----
  /** 已保存提示。 */
  SAVED: '已保存',
  /** 正在保存提示。 */
  SAVING: '正在保存',

  // ---- 历史 ----
  /** 净经验存在未确认区间时的展示。 */
  NET_EXP_UNCONFIRMED: '净经验 —（存在未确认区间）',

  // ---- 隐私声明 ----
  /** 顶部隐私提示。 */
  LOCAL_ONLY: '记录本次升级效率，游戏画面仅在当前设备处理',
} as const;

/** 提示语常量的值类型。 */
export type TextKey = keyof typeof TEXTS;
